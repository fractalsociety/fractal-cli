[package]
name = "fractal-chain"
version = "0.1.0"
edition = "2021"
description = "Per-scale append-only, hash-linked, signed receipt ledger — the Fractal Society honesty anchor (P7.1)"

[dependencies]
ed25519-dalek = "2"
sha3 = "0.10"
thiserror = "1"

[dev-dependencies]
rand_core = { version = "0.6", features = ["getrandom"] }
serde_json = "1"

[workspace]
