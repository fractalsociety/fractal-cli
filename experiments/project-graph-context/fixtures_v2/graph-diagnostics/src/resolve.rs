use crate::model::{Edge, Node, RelationGraph};

pub fn parse_graph(_input: &str) -> Result<RelationGraph, &'static str> {
    Err("parser_pending")
}

pub fn resolve_label(_graph: &RelationGraph, _label: &str) -> Result<String, &'static str> {
    Err("resolver_pending")
}

pub fn run_json(_input: &str, _query: &str) -> Result<String, &'static str> {
    Err("resolver_pending")
}

